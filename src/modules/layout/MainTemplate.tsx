import { FC } from 'react'

export const MainTemplateHeader: FC = ({ children }) => {
  return <div>{children}</div>
}

export const MainTemplateBody: FC = ({ children }) => {
  return <div>{children}</div>
}

export const MainTemplate: FC = ({ children }) => {
  return <div>{children}</div>
}
